

## drop_duplicates  

### 정의  

- 중복이 있는 row 들에 대해, 중복을 제거하고 하나만 남긴다.  
- 기본적으로 중복 row들 중 정렬상 위쪽에 있는 row 하나만을 유지하며, 그 외의 row는 제거한다.  
- 혹은 keep 인자를 이용해 유지할 row를 선택할 수 있다.  

### 문법  

```python
pre_jeongwa_df.drop_duplicates(subset=["STUDENT_NO"], keep="first", inplace=True)
```