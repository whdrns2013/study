

## 데이터 불러오기  

### 값의 자료형을 지정해서 데이터 불러오기  

#### 모든 컬럼의 자료형을 동일하게  

```python
df = pd.read_csv(file_path, dtype=자료형)
```

#### 특정 컬럼의 자료형만 지정  

```python
df = pd.read_csv(file_path, dtype={"col_name" : 자료형})
```