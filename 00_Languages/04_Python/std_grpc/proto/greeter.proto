syntax = "proto3";

// 서버와 클라이언트가 통신할 서비스 정의
service Greeter {
  rpc SayHello (HelloRequest) returns (HelloReply);
}

// 요청 메시지 구조
message HelloRequest {
  string name = 1;
}

// 응답 메시지 구조
message HelloReply {
  string message = 1;
}